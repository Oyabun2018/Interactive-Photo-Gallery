
const pictureSearch = document.getElementById('pictureSearch');
pictureSearch.addEventListener('keyup', e => {
    let currentValue = e.target.value.toLowerCase();
    let keyword = document.querySelectorAll('a.data-caption')
});